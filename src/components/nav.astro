---
const base = import.meta.env.BASE_URL; // "/" en dev, "/capitoken-site/" en build

const LINKS = [
  { label: "Home", href: "/" },
  { label: "Launch", href: "#launch" },
  { label: "Tokenomics", href: "#tokenomics" },
  { label: "Roadmap", href: "#roadmap" },
  { label: "Links", href: "/links" },
  { label: "Verify", href: "/verify" },
  { label: "Checklist", href: "/checklist" },
  { label: "FAQ", href: "#faq" },
];

function fixHref(href) {
  if (href.startsWith("#")) return href;        // anchors
  if (href.startsWith("http")) return href;     // absolutos

  // Internos: asegurar base
  const clean = href.startsWith("/") ? href.slice(1) : href;
  return `${base}${clean}`.replace(/\/{2,}/g, "/");
}
---

<header class="nav">
  <div class="nav__inner container">
    <a class="brand" href={base}>
      <span class="brand__dot" aria-hidden="true"></span>
      <span class="brand__name">Capitoken</span>
    </a>

    <nav class="nav__links" aria-label="Primary">
      {LINKS.map((l) => (
        <a href={fixHref(l.href)}>{l.label}</a>
      ))}
    </nav>
  </div>
</header>
